name: Build y publicar APK

# Se dispara con cada push a main: compila el APK debug (firmado con la
# llave fija de keystore/wakeup-sideload.jks) y lo publica como GitHub
# Release. La app puede revisar el release más reciente desde Ajustes →
# Buscar actualizaciones sin que haya que pasar el APK a mano cada vez.
on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Historial completo: el versionCode se calcula como cantidad de commits
          # (git rev-list --count HEAD), y con clone superficial siempre daría 1.
          fetch-depth: 0

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache de Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle.kts', 'gradle/libs.versions.toml') }}

      - name: Dar permiso de ejecución a gradlew
        run: chmod +x gradlew

      - name: Calcular número de versión
        id: version
        run: echo "code=$(git rev-list --count HEAD)" >> "$GITHUB_OUTPUT"

      - name: Compilar APK debug
        run: |
          ./gradlew assembleDebug --no-daemon \
            -PappVersionCode=${{ steps.version.outputs.code }} \
            -PappVersionName=1.1.${{ steps.version.outputs.code }}

      - name: Publicar release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ steps.version.outputs.code }}
          name: "Wake up · build ${{ steps.version.outputs.code }}"
          body: |
            Build automática desde el commit ${{ github.sha }}.
            Instálala directo en tu Android: descarga el .apk de abajo y ábrelo,
            o usa "Buscar actualizaciones" dentro de la app.
          make_latest: true
          files: app/build/outputs/apk/debug/wake-up.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
